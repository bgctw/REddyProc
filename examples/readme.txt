This directory holds examples used in REddyProc testing and code examples

Those examples are not packaged with REddyProc due to package size constraints.
On usage by "getExamplePath", the examples are downloaded from github into a
temporary directory.
